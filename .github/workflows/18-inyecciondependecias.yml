name: 18 - Inyección de código
on:
  issues: 
    types:
      - opened
jobs:
  inyeccion:
    runs-on: ubuntu-latest
    env:
      ISSUE_TITLE: ${{ github.event.issue.title }}
    steps:
      - run: |
          echo ${ISSUE_TITLE}
          